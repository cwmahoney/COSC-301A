#include <pthread.h>

struct node{
 	pthread_t thread;
    struct node *next;
};

void insert(pthread_t thread, struct node **head, struct node **tail);
void clear_list(struct node *curnode);
void killHead(struct node **head_ptr, struct node **tail_ptr);
